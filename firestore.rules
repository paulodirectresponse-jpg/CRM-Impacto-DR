rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Business data is server-only. The browser authenticates with Firebase Auth
    // and accesses CRM data exclusively through the authenticated Express API.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
